# Core dependencies
gymnasium>=0.29.0
pygame>=2.5.0
numpy>=1.24.0,<2.0.0  

# Stable-Baselines3 for RL algorithms
stable-baselines3[extra]>=2.2.0
torch>=2.0.0

# Logging and monitoring (compatible versions)
tensorboard>=2.16.0,<2.17.0  # Match tensorflow requirement
protobuf>=3.20.3,<5.0.0  # Match tensorflow requirement
# wandb>=0.15.0  # Comment out if causing issues

# Additional utilities
matplotlib>=3.7.0
scipy>=1.10.0
tqdm>=4.65.0

# TensorFlow dependencies (if missing)
astunparse>=1.6.0
flatbuffers>=23.5.26
gast>=0.2.1,!=0.5.0,!=0.5.1,!=0.5.2
google-pasta>=0.1.1
h5py>=3.10.0
keras>=3.0.0
libclang>=13.0.0
ml-dtypes~=0.3.1
opt-einsum>=2.3.2
termcolor>=1.1.0
wrapt>=1.11.0
